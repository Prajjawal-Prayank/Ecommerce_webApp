{
    "scripts": {
        "client": "cd ecommerce-frontend && npm install && npm start",
        "server": "cd ecommerce && npm install && npm start",
        "start": "npm run server"
        },
    "engines": {
        "node": "11.6.0"
        } ,
    "dependencies":{
        "concurrently":"^5.2.0"
    }       
}
